- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/.config/ssh:
      recursive: true

- link:
    ~/.config/ssh/config: ssh/config

- shell:
    - [git submodule update --init --recursive, Installing submodules]
