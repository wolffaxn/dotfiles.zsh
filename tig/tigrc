# '--all'   - show all refs in /refs
# '-n 1000' - limit number of commits loaded to 1000
set main-options = --all -n 1000

# don't show staged and unstaged changes in the main view
set show-changes = no
